<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="golhar.cocomo.zinger">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        tools:replace="android:theme">
        <activity android:name=".CartActivity"/>
        <activity android:name=".UpdateUserProfileActivity"></activity>
        <activity android:name=".SearchItemShopActivity" />
        <activity android:name=".ShopMenuItemListActivity" />
        <activity android:name=".OnBoardingActivity" />
        <activity android:name=".ShopListActivity" />
        <activity android:name=".SellerActivity" />
        <activity android:name=".OrderHistoryActivity" />
        <activity android:name=".CollegeListActivity" />
        <activity android:name=".RecyclerViewDemoActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".OtpVerificationActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".OrderHistoryItemDetailActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>